import React, { FC, useMemo } from 'react';

const AppIndex: FC = props => {
  return useMemo(() => {
    return (
      <div>
        <div>app Indexwede 1233233 </div>
      </div>
    );
  }, []);
};

export default AppIndex;
